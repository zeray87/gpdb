#a demo for extension

    An new extension called 'strcmp'

    Usage:
        select strcmp('a','a'); //return 't';
        select strcmp('a','b'); //return 'f';
        select text(strcmp('a','b')); //return 'false';

    Co-authors: Zeray, Surafel, Lalisa, Tsehayu, Wuberest
